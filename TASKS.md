**Task 1: ES6 Features Implementation**

_Objective:_


  Implement a simple JavaScript program demonstrating the following ES6 features:
  
  Use let and const for variable declarations
  
  Create arrow functions for array operations
  
  Use classes to define simple objects
  
  Utilize array methods like map() and filter() for data manipulation

_Goal:_
  Create a program that integrates all these ES6 features in a meaningful way.


**Task 2: AJAX Pagination Implementation**

_Objective:_


Create a web page that:

Fetches data from a JSON API using AJAX

Implements pagination to display data in chunks (e.g., 5 items per page)

Uses ES6 features (arrow functions, let/const) in the implementation

_Goal:_
Demonstrate how to dynamically load and display data from an API while keeping the code modern and readable.

_Submission:_

Include your complete code

Provide a brief explanation of your implementation


**Task 3: JavaScript Form Validation**

_Objective_
Create a web page with a user registration form that includes the following fields:

- Name  
- Email  
- Password  

Implement **client-side validation** using JavaScript to ensure:

- All fields are required  
- Email format is valid  
- Password has a minimum length (e.g., 8 characters)

Use ES6 features such as:

- `let` and `const` for variable declarations  
- Arrow functions for event handling  
- Template literals for displaying validation messages  

_Goal:_
Demonstrate how JavaScript can be used to validate user input before submitting a form.

---

**Task 4: Fetch API Data Display**

_Objective_
Create a web page that:

- Uses the **Fetch API** to retrieve data from a public API (e.g., users, posts, or products)
- Displays the retrieved data in a structured format such as a **list** or **table**
- Uses ES6 features such as **arrow functions**, **destructuring**, and **template literals**

_Goal:_
Show how modern JavaScript can be used to retrieve and display remote data efficiently.

---

**Task 5: Dynamic To-Do List Application**

_Objective_
Develop a simple **To-Do List web application** that allows users to:

- Add new tasks  
- Mark tasks as completed  
- Delete tasks  

_Requirements_
- Use **ES6 classes** to represent tasks or the task manager  
- Use array methods like `map()`, `filter()`, or `forEach()` for managing tasks  
- Use **arrow functions** for event listeners  
_Goal:_
Demonstrate the use of modern JavaScript concepts to build a small interactive web application.

---

## Submission Requirements

- Include your **complete code**
- Provide a **brief explanation** of your implementation
- Add **screenshots of your working application** (optional)

**Task 6: Local Storage Notes Application**

_Objective_

Create a simple **Notes web application** that allows users to:

- Add new notes  
- View saved notes  
- Delete notes  

All notes should be **stored in the browser using Local Storage** so that they remain available even after the page is refreshed.

_Requirements_

- Use **ES6 classes** to manage notes or the notes manager  
- Use the **Local Storage API** to save and retrieve notes  
- Use array methods like `map()`, `filter()`, or `forEach()` to manage note data  
- Use **arrow functions** for event handling  
- Use `let` and `const` for variable declarations  

_Goal:_  
Demonstrate how JavaScript can be used to build a small persistent application using **Local Storage and modern ES6 features**.
```
